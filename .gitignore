# Brood package cache — regenerable from project.lock.blsp (which is committed)
_deps/

# Editor / OS cruft
*.swp
.DS_Store

# Generated documentation site (`nest docs`) and the check/image cache
doc/
.brood/
.brood_crash_dump
